import { AnnShopValidators } from './ann-shop-validators';

describe('AnnShopValidators', () => {
  it('should create an instance', () => {
    expect(new AnnShopValidators()).toBeTruthy();
  });
});
